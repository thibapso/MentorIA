generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model SkillsMatch {
  id           Int    @id @default(autoincrement())
  area         String
  competencias String
  
  @@map("skills_match")
}
